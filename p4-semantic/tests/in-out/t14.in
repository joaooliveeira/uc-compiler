int f(int m, int n) {
    if (! (n >= 2)) {
        n -= 2;
        return n;
    }
    else {
        m--;
        return m;
    }
}

int main() {
    int m = -9;
    float n = -4.0;
    print(f(m, n));
    return 0;
}
